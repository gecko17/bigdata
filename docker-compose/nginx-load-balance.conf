user root root;
worker_processes 2;

events {
    worker_connections  1024;
}

http {
    upstream myapp1 {
        server web_1:5000;
        server web_2:5000;
    }

    server {
        listen 5000;

        location / {
            proxy_pass http://myapp1;
        }
    }
}